# CI skeleton — stack-agnostic. The bootstrap fills the lint/test/build steps
# for the project's actual stack (detected at setup time). Pinned setup actions
# for the common stacks are included as commented examples; delete what you do
# not use. Resolve fresh SHAs with the procedure in references/action-pins.md.

name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      # --- Node / TypeScript (example) ---
      # - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      # - run: npm ci
      # - run: npm run lint
      # - run: npm test

      # --- Python (example) ---
      # - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
      #   with:
      #     python-version: '3.x'
      # - run: pip install -e '.[dev]'   # or: uv sync
      # - run: ruff check .
      # - run: pytest

      - name: Placeholder
        run: echo "Replace with the project's lint/test/build steps."
